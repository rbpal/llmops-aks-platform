# AKS-only: Azure Monitor Managed Prometheus scrape config for genai-api.
# TODO(step_06_task03): apply after make up-aks.
apiVersion: v1
kind: ConfigMap
metadata:
  name: ama-metrics-prometheus-config
  namespace: kube-system
data:
  prometheus-config: |
    scrape_configs:
      - job_name: genai-api
        kubernetes_sd_configs: [{ role: pod }]
        # TODO(step_06_task03): relabel to scrape /metrics on the app pods
